# requirements.txt

# This project has no Python dependencies.
# Ensure 'nmap' is installed on your system to use vinmap.

# Example installation commands for Nmap:
# On Debian/Ubuntu:
#   sudo apt-get install nmap
#
# On macOS using Homebrew:
#   brew install nmap
#
# On Windows:
#   Download the installer from https://nmap.org/download.html and follow the installation instructions.

